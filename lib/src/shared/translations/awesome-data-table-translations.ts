export interface AwesomeDataTableTranslations {
    indexColumn: string;
    selectColumn: string;
    expandColumn: string;
    columnsMenuHeaderText: string;
    rowsMenuHeaderText: string;
    paginationResult(offset: number, current: number, max: number);
}
